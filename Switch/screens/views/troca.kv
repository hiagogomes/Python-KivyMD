<Troca>:
    MDSwitch:
        pos_hint: {'center_x': .5, 'center_y': .5}
        width: dp(45)
        # Ação executada quando o botão for pressionado, trocando a cor de fundo.
        # Chama a função check na classe troca.
        on_active: app.check(*args)
